; Session: tetap login ~1 hari (tenant app + portal)
; Cookie bertahan saat app/browser ditutup + server tak hapus sesi <1 hari.
; Diblokir dari akses web via .htaccess (dotfile + *.ini denied).
session.gc_maxlifetime = 86400
session.cookie_lifetime = 86400
